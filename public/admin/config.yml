# yaml-language-server: $schema=https://unpkg.com/@sveltia/cms/schema/sveltia-cms.json

backend:
  name: github
  repo: breyes-oss/texas-gulf-bank
  branch: main
  base_url: https://tgbtest2.pages.dev
  auth_endpoint: /api/auth
  site_domain: tgbtest2.pages.dev

media_folder: public/blog
public_folder: /blog

# Editorial workflow is not yet supported in Sveltia CMS.
# Content publishes as direct commits to the configured branch.

collections:
  - name: blog
    label: Blog Posts
    label_singular: Blog Post
    folder: src/content/blog
    create: true
    delete: true
    slug: "{{slug}}"
    preview_path: blog/{{slug}}
    extension: md
    format: frontmatter
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Publish Date, name: date, widget: datetime, format: MMMM D, YYYY }
      - { label: Featured Image, name: image, widget: image, required: false }
      - { label: Category, name: category, widget: select, options: [Business, Community, Security, Personal] }
      - { label: Excerpt, name: excerpt, widget: text, required: false }
      - label: Show end-of-article CTA
        name: showCta
        widget: boolean
        default: true
        required: false
        hint: "Adds a call-to-action card after the article body."
      - label: End-of-article CTA
        name: cta
        widget: object
        required: false
        collapsed: true
        fields:
          - { label: Eyebrow, name: eyebrow, widget: string, required: false, default: "Next step" }
          - { label: Title, name: title, widget: string, required: false }
          - { label: Body, name: body, widget: text, required: false }
          - { label: Primary button label, name: primaryLabel, widget: string, required: false, default: "Find a Branch" }
          - { label: Primary button URL, name: primaryHref, widget: string, required: false, default: "https://www.texasgulfbank.com/locations/" }
          - { label: Secondary button label, name: secondaryLabel, widget: string, required: false, default: "Call 800.467.7216" }
          - { label: Secondary button URL, name: secondaryHref, widget: string, required: false, default: "tel:8004677216" }
      - label: Body
        name: body
        widget: markdown
        hint: "Optional shortcodes — on their own line: {{rates}} for the live rate table, {{cta}} for an in-body CTA. Customize with attributes like {{cta title=\"Open an account\" primaryLabel=\"Get started\" primaryHref=\"https://...\"}}"

  - name: pages
    label: Pages
    label_singular: Page
    folder: src/content/pages
    create: true
    delete: false
    extension: yaml
    format: yaml
    identifier_field: title
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Hero Tagline, name: hero_tagline, widget: string, required: false }
      - { label: Hero Headline, name: hero_headline, widget: string, required: false }
      - { label: Hero Headline Accent, name: hero_headline_accent, widget: string, required: false }
      - { label: Hero Subtext, name: hero_subtext, widget: text, required: false }
      - { label: Hero Primary Button, name: hero_primary_cta, widget: string, required: false }
      - { label: Hero Secondary Button, name: hero_secondary_cta, widget: string, required: false }
      - { label: Story Headline, name: story_headline, widget: string, required: false }
      - { label: Story Headline Accent, name: story_headline_accent, widget: string, required: false }
      - { label: Story Body 1, name: story_body_1, widget: text, required: false }
      - { label: Story Body 2, name: story_body_2, widget: text, required: false }
      - { label: Stat 1 Number, name: story_stat_1_number, widget: string, required: false }
      - { label: Stat 1 Label, name: story_stat_1_label, widget: string, required: false }
      - { label: Stat 2 Number, name: story_stat_2_number, widget: string, required: false }
      - { label: Stat 2 Label, name: story_stat_2_label, widget: string, required: false }
      - { label: Stat 3 Number, name: story_stat_3_number, widget: string, required: false }
      - { label: Stat 3 Label, name: story_stat_3_label, widget: string, required: false }
      - { label: CTA Headline, name: cta_headline, widget: string, required: false }
      - { label: CTA Headline Accent, name: cta_headline_accent, widget: string, required: false }
      - { label: CTA Subtext, name: cta_subtext, widget: text, required: false }
      - { label: CTA Primary Button, name: cta_primary_button, widget: string, required: false }
      - { label: CTA Phone, name: cta_phone, widget: string, required: false }

  - name: data
    label: Site Data
    label_singular: Data file
    folder: src/content/data
    create: false
    delete: false
    extension: yaml
    format: yaml
    identifier_field: title
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Published date label, name: published, widget: string, required: false, hint: "Shown as “Featured APYs published …”" }
      - { label: Disclaimer, name: disclaimer, widget: text, required: false }
      - { label: Full rates URL, name: full_rates_url, widget: string, required: false }
      - label: Rate rows
        name: items
        widget: list
        summary: "{{fields.name}} — {{fields.value}}"
        fields:
          - { label: Product name, name: name, widget: string }
          - { label: Detail / minimum, name: detail, widget: string, required: false }
          - { label: APY value, name: value, widget: string, hint: "Example: 3.45%" }

  - name: locations
    label: Locations
    label_singular: Location
    folder: src/content/locations
    create: true
    delete: true
    extension: yaml
    format: yaml
    identifier_field: title
    slug: "{{name}}"
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Location name, name: name, widget: string }
      - { label: Region / county, name: region, widget: string, hint: "Example: Brazoria County" }
      - { label: Street address, name: street, widget: string }
      - { label: Suite, name: suite, widget: string, required: false }
      - { label: City, name: city, widget: string }
      - { label: State, name: state, widget: string, default: TX }
      - { label: ZIP, name: zip, widget: string }
      - { label: Phone, name: phone, widget: string, hint: "Format: 979-849-2372" }
      - { label: Has ATM, name: has_atm, widget: boolean, default: true, required: false }
      - { label: Has drive-thru, name: has_drive_thru, widget: boolean, default: true, required: false }
      - label: ATM types
        name: atm_types
        widget: list
        required: false
        summary: "{{fields.value}}"
        fields:
          - { label: Type, name: value, widget: string }
      - label: Services
        name: services
        widget: list
        required: false
        summary: "{{fields.value}}"
        fields:
          - { label: Service, name: value, widget: string }
      - label: Languages spoken
        name: languages
        widget: list
        required: false
        summary: "{{fields.value}}"
        fields:
          - { label: Language, name: value, widget: string }
      - { label: Latitude, name: lat, widget: number, hint: "Decimal degrees, e.g. 29.1822543" }
      - { label: Longitude, name: lng, widget: number, hint: "Decimal degrees, e.g. -95.4338758" }
      - { label: Exterior photo, name: photo, widget: image, required: false, hint: "Upload an exterior photo of this location (optional)" }
      - label: Hours of operation
        name: hours
        widget: object
        required: false
        fields:
          - label: Lobby
            name: lobby
            widget: list
            required: false
            summary: "{{fields.days}} {{fields.open}} – {{fields.close}}"
            fields:
              - { label: Days, name: days, widget: string, hint: "Example: Mon - Fri or Sat" }
              - { label: Open (display), name: open, widget: string, hint: "Example: 9 AM" }
              - { label: Close (display), name: close, widget: string, hint: "Example: 5 PM" }
              - { label: Open 24h, name: open24, widget: string, required: false, hint: "Example: 09:00 — used for open-now status" }
              - { label: Close 24h, name: close24, widget: string, required: false, hint: "Example: 17:00 — used for open-now status" }
          - label: Drive-thru
            name: drive_thru
            widget: list
            required: false
            summary: "{{fields.days}} {{fields.open}} – {{fields.close}}"
            fields:
              - { label: Days, name: days, widget: string, hint: "Example: Mon - Fri or Sat" }
              - { label: Open (display), name: open, widget: string, hint: "Example: 8 AM" }
              - { label: Close (display), name: close, widget: string, hint: "Example: 5 PM" }
              - { label: Open 24h, name: open24, widget: string, required: false, hint: "Example: 08:00 — used for open-now status" }
              - { label: Close 24h, name: close24, widget: string, required: false, hint: "Example: 17:00 — used for open-now status" }
      - { label: Notes / special hours, name: notes, widget: text, required: false, hint: "Holiday, weather, or temporary changes" }


  - name: fees
    label: Fee Schedule
    label_singular: Fee Schedule
    folder: src/content/fees
    create: false
    delete: false
    extension: yaml
    format: yaml
    identifier_field: title
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Effective / published note, name: published, widget: string, required: false }
      - { label: Disclaimer, name: disclaimer, widget: text, required: false }
      - { label: Full schedule URL, name: full_schedule_url, widget: string, required: false }
      - label: Fee sections
        name: sections
        widget: list
        summary: "{{fields.name}}"
        fields:
          - { label: Section name, name: name, widget: string }
          - { label: Note (shown under section title), name: note, widget: text, required: false }
          - label: Fee rows
            name: items
            widget: list
            summary: "{{fields.name}} — {{fields.value}}"
            fields:
              - { label: Fee name, name: name, widget: string }
              - { label: Detail / footnote, name: detail, widget: text, required: false }
              - { label: Amount, name: value, widget: string, hint: "Example: $30.00 each or No fee" }
